/* ============================================================
   第一カード商会 アドウォールLP ― CSS
   ブラウザエディターの「CSS」欄にこのファイルの中身をそのまま貼り付けてください。
   (index.html / login.html 共通)
   ============================================================ */

/* =========================================================
   第一カード商会 - アドウォールLP
   ========================================================= */

/* ---------- Design tokens ---------- */
:root{
  --pink:        #f0509e;
  --pink-deep:   #e5388c;
  --pink-dark:   #d62e7a;
  --pink-light:  #fce4ef;
  --pink-bg:     #fbe3ee;
  --magenta:     #f5559e;
  --teal:        #33b4a0;
  --teal-dark:   #2a9d8a;
  --teal-bg:     #e3f4ef;
  --gold:        #f4b21e;
  --gold-dark:   #e09a12;
  --gold-deep:   #c9820a;
  --ink:         #4a4a4a;
  --text:        #5a5a5a;
  --white:       #ffffff;
  --radius:      16px;
  --container:   1000px;
  --shadow-card: 0 6px 20px rgba(224, 96, 150, .12);
}

/* ---------- Reset ---------- */
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth;-webkit-text-size-adjust:100%}
body{
  font-family:"M PLUS Rounded 1c",-apple-system,"Hiragino Kaku Gothic ProN","Yu Gothic",Meiryo,sans-serif;
  color:var(--text);
  line-height:1.75;
  background:var(--white);
  font-weight:500;
  overflow-x:hidden;
}
img{max-width:100%;height:auto;display:block}
a{color:inherit;text-decoration:none}
ul,ol{list-style:none}
b{font-weight:800}

.container{width:100%;max-width:var(--container);margin-inline:auto;padding-inline:20px}

.hl-pink{color:var(--pink-deep);font-weight:800}
.hl-teal{color:var(--teal-dark);font-weight:800}
.note-inline{font-size:.8em;color:#8a8a8a;font-weight:500}
.note-center{text-align:center;font-size:.82rem;color:#8a8a8a;margin-top:18px}
.note-left{font-size:.8rem;color:#9a9a9a;margin-top:14px}

/* ---------- Coins ---------- */
.coin{
  display:inline-grid;place-items:center;
  width:44px;height:44px;border-radius:50%;
  background:radial-gradient(circle at 34% 30%, #ffe38a 0%, var(--gold) 45%, var(--gold-dark) 78%, var(--gold-deep) 100%);
  color:#fff;font-weight:900;font-size:1.3rem;
  border:2px solid #fff5cf;
  box-shadow:0 3px 6px rgba(180,120,0,.35), inset 0 -3px 6px rgba(150,90,0,.35);
  text-shadow:0 1px 1px rgba(150,90,0,.5);
}
.coin-sm{width:30px;height:30px;font-size:.95rem;border-width:2px}
.coin-float{position:absolute;width:52px;height:52px;font-size:1.5rem;z-index:2}

/* ---------- Buttons ---------- */
.btn{
  display:inline-flex;align-items:center;gap:12px;
  padding:16px 30px;border-radius:999px;
  font-weight:900;font-size:1.15rem;letter-spacing:.02em;
  cursor:pointer;border:none;position:relative;
  transition:transform .12s ease, box-shadow .12s ease, filter .12s ease;
}
.btn .btn-arrow{font-size:1.5rem;line-height:1;margin-left:2px}
.btn-label{white-space:nowrap}

.btn-primary{
  background:linear-gradient(180deg,#ff6aae 0%, var(--pink) 55%, var(--pink-deep) 100%);
  color:#fff;
  border:3px solid #fff;
  box-shadow:0 7px 0 var(--pink-dark), 0 14px 24px rgba(214,46,122,.4);
  text-shadow:0 2px 2px rgba(150,20,80,.35);
}
.btn-primary:hover{transform:translateY(2px);box-shadow:0 5px 0 var(--pink-dark),0 10px 18px rgba(214,46,122,.4)}
.btn-primary:active{transform:translateY(7px);box-shadow:0 0 0 var(--pink-dark),0 4px 10px rgba(214,46,122,.35)}

.btn-white{
  background:#fff;color:var(--pink-deep);
  border:3px solid #ffd6e8;
  box-shadow:0 7px 0 #d98cb2, 0 14px 24px rgba(120,20,70,.25);
}
.btn-white:hover{transform:translateY(2px);box-shadow:0 5px 0 #d98cb2,0 10px 18px rgba(120,20,70,.25)}
.btn-white:active{transform:translateY(7px);box-shadow:0 0 0 #d98cb2}

.btn-primary .coin,.btn-white .coin{box-shadow:0 2px 4px rgba(180,120,0,.4)}

/* pulse */
@keyframes cta-pulse{0%,100%{filter:brightness(1)}50%{filter:brightness(1.08)}}
.hero .btn-primary{animation:cta-pulse 2.4s ease-in-out infinite}

/* ---------- Header ---------- */
.site-header{
  position:sticky;top:0;z-index:50;
  background:rgba(255,255,255,.96);
  backdrop-filter:blur(6px);
  transition:box-shadow .2s ease;
}
.site-header.scrolled{box-shadow:0 2px 14px rgba(224,96,150,.18)}
.header-inner{display:flex;align-items:center;justify-content:space-between;gap:12px;padding-block:10px}
.brand{display:flex;align-items:center;gap:11px}
.brand-logo{width:auto;height:46px;object-fit:contain;flex:0 0 auto}
.brand-name{display:flex;flex-direction:column;line-height:1.12}
.brand-name-ja{font-weight:900;font-size:1.18rem;color:#2b3a55;letter-spacing:.06em}
.brand-name-en{font-size:.54rem;letter-spacing:.13em;color:#8a94a6;font-weight:700}

/* ログイン済みページ: マイページへのリンク(ログイン状態の目印を兼ねる) */
.mypage-link{
  display:inline-flex;align-items:center;gap:6px;flex:0 0 auto;
  padding:8px 16px 8px 12px;border-radius:999px;
  background:var(--teal-bg);color:var(--teal-dark);
  font-weight:800;font-size:.82rem;letter-spacing:.02em;
  border:1.5px solid rgba(42,157,138,.25);
  transition:background .15s ease, transform .12s ease;
}
.mypage-link:hover{background:#d5efe8;transform:translateY(-1px)}
.mypage-link:active{transform:translateY(0)}
.mypage-icon{display:inline-flex;width:18px;height:18px;flex:0 0 auto}
.mypage-icon svg{width:100%;height:100%}
.mypage-dot{
  display:inline-block;width:7px;height:7px;border-radius:50%;
  background:var(--teal-dark);flex:0 0 auto;
  box-shadow:0 0 0 3px rgba(42,157,138,.18);
}
.mypage-text{white-space:nowrap}
@media (max-width:420px){
  .mypage-link{padding:7px 12px 7px 10px;font-size:.72rem;gap:4px}
  .mypage-icon{width:16px;height:16px}
}
@media (max-width:380px){
  /* 社名と競合しないよう、アイコン+ドットのみの丸バッジに縮小 */
  .mypage-link{padding:8px;gap:0}
  .mypage-text{
    position:absolute;width:1px;height:1px;overflow:hidden;
    clip:rect(0,0,0,0);white-space:nowrap;
  }
}

/* ---------- Hero ---------- */
.hero{
  position:relative;overflow:hidden;
  background:
    radial-gradient(ellipse at 68% 34%, rgba(255,255,255,.85) 0%, rgba(255,255,255,0) 46%),
    linear-gradient(160deg,#ffd9ea 0%, #fbc4dd 45%, #f6b2d3 100%);
  padding-block:34px 0;
}
.hero .spark{position:absolute;color:#fff;opacity:.8;font-size:1.4rem;pointer-events:none;animation:twinkle 3s ease-in-out infinite}
.hero .s1{top:12%;left:8%}
.hero .s2{top:60%;left:4%;font-size:1rem;animation-delay:.6s}
.hero .s3{top:22%;right:6%;font-size:1.8rem;animation-delay:1.1s}
.hero .s4{top:74%;right:12%;animation-delay:1.7s}
@keyframes twinkle{0%,100%{opacity:.25;transform:scale(.8)}50%{opacity:.9;transform:scale(1.15)}}

.hero-inner{display:grid;grid-template-columns:1.05fr .95fr;align-items:end;gap:20px}
.hero-copy{padding-bottom:38px}

.hero-badge{
  display:inline-block;
  background:#fff;color:var(--pink-deep);
  font-weight:800;font-size:.92rem;
  padding:7px 20px;border-radius:999px;
  box-shadow:0 3px 8px rgba(224,96,150,.22);
  position:relative;margin-bottom:16px;
}
.hero-badge::after{
  content:"";position:absolute;left:34px;bottom:-7px;
  border:8px solid transparent;border-top-color:#fff;border-bottom:0;
}

.hero-title{
  font-weight:900;line-height:1.05;letter-spacing:-.01em;
  font-size:clamp(2.7rem,8.5vw,4.4rem);
  margin-bottom:18px;
  -webkit-text-stroke:.09em #fff;
  paint-order:stroke fill;
  text-shadow:0 5px 0 rgba(196,60,120,.18);
}
.hero-title span{display:inline-block}
.t-pink{color:var(--pink)}
.t-ink{color:#5b4b52;font-size:.62em;-webkit-text-stroke-width:.09em}
.t-teal{color:#2fb6b0}
.t-gold{color:var(--gold);font-style:italic;transform:rotate(-4deg);text-shadow:0 4px 0 var(--gold-deep)}

.hero-lead{color:#5a5a5a;font-weight:700;font-size:1.02rem;margin-bottom:10px}
.hero-copy .btn{margin-top:16px}
.hero-note{margin-top:10px;font-size:.78rem;color:#8a7268;font-weight:600}

.hero-illust{position:relative;align-self:end;min-height:120px}
.hero-illust img{margin-left:auto;max-height:440px;width:auto;filter:drop-shadow(0 8px 16px rgba(180,80,130,.25))}
.hero-illust.illust-fallback{
  min-height:300px;border:2px dashed rgba(255,255,255,.85);border-radius:16px;
  background:rgba(255,255,255,.28);
  display:grid;place-items:center;margin-bottom:20px;
}
.hero-illust.illust-fallback::after{content:"キャラクター画像\A assets/character-hero.png";white-space:pre;text-align:center;color:#b76f97;font-weight:800;font-size:.85rem}
.hero-illust .f1{top:4%;left:-2%}
.hero-illust .f2{top:58%;right:-4%}
.hero-illust .f3{bottom:10%;left:2%}
@keyframes float-y{0%,100%{transform:translateY(0)}50%{transform:translateY(-10px)}}
.coin-float{animation:float-y 3s ease-in-out infinite}
.hero-illust .f2{animation-delay:.8s}
.hero-illust .f3{animation-delay:1.5s}

/* ---------- Sections ---------- */
.section{padding-block:44px}
.about{background:linear-gradient(180deg,#e6f5f0,#def2ec)}
.cases{background:var(--pink-bg)}

/* ---------- Ribbon headings ---------- */
.ribbon{
  display:block;width:max-content;max-width:100%;margin:0 auto 26px;
  padding:9px 40px;border-radius:8px;
  color:#fff;font-weight:900;font-size:1.3rem;letter-spacing:.03em;
  text-align:center;position:relative;
  clip-path:polygon(4% 0,96% 0,100% 50%,96% 100%,4% 100%,0 50%);
}
.ribbon-teal{background:linear-gradient(180deg,#3fc0ac,var(--teal-dark))}
.ribbon-pink{background:linear-gradient(180deg,#ff77b4,var(--pink-deep))}
.ribbon-sm{font-size:1.1rem;padding:8px 30px;margin-bottom:18px}
.ribbon-spark{opacity:.85;margin-inline:10px;font-size:.8em;vertical-align:middle}

/* ---------- About ---------- */
.about-body{
  display:flex;align-items:center;gap:24px;
  background:#fdf6e9;border:2px solid #f0e1bf;border-radius:var(--radius);
  padding:24px 28px;box-shadow:var(--shadow-card);
}
.about-illust{flex:0 0 150px}
.about-illust img{max-height:190px;margin-inline:auto}
.about-illust.illust-fallback{
  height:170px;border:2px dashed #cfc199;border-radius:12px;
  display:grid;place-items:center;background:#fbf1da;
}
.about-illust.illust-fallback::after{content:"assets/\A character-point.png";white-space:pre;text-align:center;color:#b0a06f;font-weight:800;font-size:.72rem}
.about-text p{font-weight:700;color:#5a5a5a;margin-bottom:8px}
.about-text p:last-child{margin-bottom:0}

/* ---------- Case grid ---------- */
.case-grid{display:grid;grid-template-columns:repeat(6,1fr);gap:14px}
.case-card{
  background:#fff;border:2px solid #f7cade;border-radius:14px;
  padding:18px 8px 14px;text-align:center;
  display:flex;flex-direction:column;align-items:center;gap:10px;
  box-shadow:0 4px 12px rgba(224,96,150,.1);
}
.case-icon{width:44px;height:44px;color:var(--pink-deep)}
.case-icon svg{width:100%;height:100%}
.case-name{font-weight:800;font-size:.82rem;color:var(--pink-deep);line-height:1.3}

/* ---------- Two column ---------- */
.two-col-grid{display:grid;grid-template-columns:1fr 1fr;gap:22px}
.panel{
  background:#fff;border-radius:var(--radius);overflow:hidden;
  box-shadow:var(--shadow-card);border:1px solid #f3d9e6;
}
.panel-head{
  color:#fff;font-weight:900;font-size:1.15rem;text-align:center;
  padding:12px;letter-spacing:.04em;position:relative;
}
.panel-head-teal{background:linear-gradient(180deg,#40c1ad,var(--teal-dark))}
.panel-head::before,.panel-head::after{content:"✦";opacity:.7;font-size:.8em;margin-inline:10px;vertical-align:middle}
.panel-body{padding:20px 22px 22px}
.two-col .panel .ribbon{margin-top:2px}

/* points highlight (獲得ポイント例: 単一レンジ表示) */
.points-highlight{
  display:flex;flex-direction:column;align-items:center;gap:10px;
  padding:18px 4px 8px;text-align:center;
}
.coin-lg{width:64px;height:64px;font-size:1.9rem;border-width:3px}
.points-range{
  font-weight:900;color:var(--gold-dark);
  font-size:clamp(1.4rem, 4.2vw, 2rem);line-height:1.2;
}
.points-sep{margin:0 4px;color:var(--gold-dark);font-weight:700}
.points-max{font-size:clamp(1.7rem, 5.2vw, 2.4rem);margin-left:2px}
.points-unit{font-size:clamp(.9rem, 2.6vw, 1.1rem);margin-left:3px;font-weight:800}

/* flow */
.flow{display:flex;flex-wrap:wrap;justify-content:space-between;gap:10px 4px}
.flow-step{
  flex:1 1 0;min-width:80px;text-align:center;position:relative;
  display:flex;flex-direction:column;align-items:center;gap:8px;
}
.flow-step:not(:last-child)::after{
  content:"›";position:absolute;right:-8px;top:14px;
  font-size:1.6rem;font-weight:900;color:#f3a9cd;line-height:1;
}
.flow-num{
  width:38px;height:38px;border-radius:50%;
  display:grid;place-items:center;
  background:linear-gradient(180deg,#ff7ab6,var(--pink-deep));
  color:#fff;font-weight:900;font-size:1.15rem;
  box-shadow:0 3px 7px rgba(224,96,150,.4);
}
.flow-text{font-weight:800;font-size:.76rem;color:#666;line-height:1.35}

/* note list */
.note-list li{
  position:relative;padding:8px 0 8px 26px;font-weight:700;color:#5a5a5a;
  border-bottom:1px dashed #eed3e0;
}
.note-list li:last-child{border-bottom:none}
.note-list li::before{
  content:"";position:absolute;left:2px;top:14px;
  width:14px;height:14px;border-radius:50%;
  background:var(--pink);box-shadow:0 0 0 3px #fbe0ec;
}

/* faq */
.faq-item{border-bottom:1px dashed #eed3e0}
.faq-item:last-child{border-bottom:none}
.faq-q{
  width:100%;text-align:left;background:none;border:none;cursor:pointer;
  display:flex;align-items:center;gap:10px;
  font-family:inherit;font-weight:800;color:var(--pink-deep);font-size:.98rem;
  padding:12px 24px 12px 0;position:relative;line-height:1.5;
}
.faq-mark{
  flex:0 0 auto;width:24px;height:24px;border-radius:50%;
  background:var(--pink);color:#fff;font-weight:900;font-size:.85rem;
  display:grid;place-items:center;
}
.faq-q::after{
  content:"＋";position:absolute;right:2px;top:12px;color:#e79bc1;font-weight:900;
  transition:transform .2s ease;
}
.faq-item.open .faq-q::after{content:"－"}
.faq-a{
  display:none;padding:0 0 14px 34px;font-weight:600;color:#666;font-size:.9rem;
}
.faq-item.open .faq-a{display:block}

/* ---------- Footer CTA ---------- */
.footer-cta{
  position:relative;overflow:clip;
  background:linear-gradient(180deg,var(--magenta),var(--pink-deep));
  padding-block:40px 30px;
}
.footer-cta .f4{top:26%;left:5%;animation-delay:.3s}
.footer-cta .f5{top:16%;right:16%;animation-delay:1.2s}
.footer-cta-illust{
  position:absolute;right:3%;bottom:14px;width:116px;height:auto;z-index:1;
  filter:drop-shadow(0 6px 12px rgba(120,20,70,.3));
}
.footer-cta-inner{text-align:center;position:relative;z-index:2}
.footer-cta-text{
  color:#fff;font-weight:900;font-size:clamp(1.15rem,4vw,1.6rem);
  margin-bottom:16px;text-shadow:0 2px 4px rgba(120,20,70,.35);
}

/* ---------- Site footer ---------- */
.site-footer{background:#3a3a3a;color:#cfcfcf;text-align:center;padding-block:26px;font-size:.82rem}
.foot-brand{font-weight:900;color:#fff;font-size:1rem;margin-bottom:10px}
.foot-nav{display:flex;flex-wrap:wrap;justify-content:center;gap:8px 20px;margin-bottom:12px}
.foot-nav a{color:#cfcfcf;text-decoration:underline;text-underline-offset:3px}
.foot-nav a:hover{color:#fff}
.copyright{color:#9a9a9a;font-size:.75rem}

/* ---------- Sticky CTA ---------- */
.sticky-cta{
  display:none;
  position:fixed;left:0;right:0;bottom:0;z-index:60;
  padding:10px 16px calc(10px + env(safe-area-inset-bottom));
  background:rgba(255,255,255,.9);
  backdrop-filter:blur(6px);
  box-shadow:0 -3px 14px rgba(224,96,150,.22);
  display:flex;justify-content:center;
  animation:slide-up .3s ease;
}
.sticky-cta[hidden]{display:none}
@keyframes slide-up{from{transform:translateY(100%)}to{transform:translateY(0)}}
.btn-sticky{width:100%;max-width:420px;justify-content:center;padding-block:14px;font-size:1.05rem}

/* ---------- Reveal（JSが動くときだけ初期非表示にする） ---------- */
.reveal{transition:opacity .6s ease,transform .6s ease}
.js-reveal .reveal{opacity:0;transform:translateY(24px)}
.js-reveal .reveal.in-view{opacity:1;transform:none}

/* =========================================================
   Responsive
   ========================================================= */
@media (max-width:900px){
  .case-grid{grid-template-columns:repeat(3,1fr)}
}

@media (max-width:768px){
  body{padding-bottom:80px}
  .sticky-cta:not([hidden]){display:flex}
  .footer-cta-illust{display:none}
  .two-col-grid{grid-template-columns:1fr}
  .hero-inner{grid-template-columns:1fr;text-align:center}
  .hero-copy{padding-bottom:0}
  .hero-badge::after{left:50%;margin-left:-8px}
  .hero-copy .btn{margin-inline:auto}
  .hero-illust{max-width:340px;margin:10px auto 0}
  .hero-illust img{margin-inline:auto}
  .about-body{flex-direction:column;text-align:center}
  .about-illust{flex-basis:auto}
}

@media (max-width:560px){
  .container{padding-inline:16px}
  .section{padding-block:34px}
  .brand-name-en{display:none}
  .btn{font-size:1.02rem;padding:15px 22px}
  .ribbon{font-size:1.08rem;padding:8px 26px;clip-path:polygon(6% 0,94% 0,100% 50%,94% 100%,6% 100%,0 50%)}
  .case-grid{grid-template-columns:repeat(2,1fr)}
  .flow{gap:16px 4px}
  .flow-step{flex:1 1 40%;min-width:0}
  .flow-step:nth-child(3)::after,.flow-step:not(:last-child)::after{display:none}
}

@media (prefers-reduced-motion:reduce){
  *,*::before,*::after{animation:none!important;transition:none!important}
  .js-reveal .reveal{opacity:1;transform:none}
  html{scroll-behavior:auto}
}
